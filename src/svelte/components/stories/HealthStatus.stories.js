import HealthStatus from '../HealthStatus.svelte';

export default {
    title: 'Components/HealthStatus',
    component: HealthStatus,
};

export const Green = () => ({
    Component: HealthStatus,
});
